const quizSubmissionRoutes = require("./quiz.submission.routes");
const quizSubmissionService = require("./quiz.submission.service");

module.exports = {
  quizSubmissionRoutes,
  quizSubmissionService,
};
